# A Minimized Config For Myself

A lot of configs like [https://github.com/amix/vimrc](amix/vimrc) are pretty useful and awsome, but they are kind of to large and contains a lot of functions not requred for me.

Sometimes I am kind of confusing about its strange operation.

In this repo, I will prepare as less 


## Usage

```bash
git clone git@github.com:yuikns/vimrc.git ~/.vim_runtime
cd ~/.vim_runtime
./setup.sh
```



